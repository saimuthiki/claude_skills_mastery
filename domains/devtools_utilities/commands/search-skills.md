# Search Skills Command

Search the skills registry for entities matching a query.

Usage: /search-skills $ARGUMENTS

Search across all entity names, descriptions, tags, and domains for matches.
Display results grouped by type (skills, agents, plugins, commands, hooks).
